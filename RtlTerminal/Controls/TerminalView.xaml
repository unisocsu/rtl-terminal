<UserControl x:Class="RtlTerminal.Controls.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Focusable="True"
             FocusVisualStyle="{x:Null}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource TerminalBackground}">
        <RichTextBox x:Name="OutputBox"
                     Background="Transparent"
                     Foreground="{StaticResource TerminalForeground}"
                     CaretBrush="{StaticResource TerminalForeground}"
                     BorderThickness="0"
                     FontFamily="Cascadia Mono, Consolas, Courier New"
                     FontSize="14"
                     IsReadOnly="False"
                     IsUndoEnabled="False"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="False"
                     AcceptsTab="False"
                     PreviewTextInput="OutputBox_PreviewTextInput"
                     PreviewKeyDown="OutputBox_PreviewKeyDown"
                     CommandManager.PreviewCanExecute="OutputBox_PreviewCanExecute">
        </RichTextBox>
    </Grid>
</UserControl>
